#include <iostream>
using namespace std;
int main(){
    cout << "Hello, World" << endl;
    return 0;
}
//cout输出流，是一个对象，将右边的文字等写入输出流，<<符号像一个漏斗，将右边的写入左边的，并且表现在终端上。称为向流写入。
//cin输入流，是一个对象，从键盘输入到cin，>>符号像一个漏斗，将左边输入留的写到右边的对象中。称为从流读取。