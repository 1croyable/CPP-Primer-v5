-----------------------------4.8
逻辑与运算符和逻辑或运算符都是先求左侧运算对象再求右侧运算对象，当且仅当左侧对象无法确定结果时才会计算右侧的。这叫短路求值
相等性运算符就是二元的比较
-----------------------------4.9
逻辑运算符的优先级很低的
const char *cp = "Hello World"
if (cp && *cp)
首先解引用cp，然后再计算&&的结果，cp意思是指针值，他不是空指针，所以正确，到此&&还没结束，继续判断cp指向的那个char是不是0，显然也不是，所以结果是true