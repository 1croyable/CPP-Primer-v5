-----------------------2.9
(a)
>>实际上是一个函数，名字叫 operator>> 因此不会对其中的对象进行默认初始化
>>输入运算符的工作是将输入流istream中的数据赋值给>>右边的变量，而对变量赋值并不是初始化工作。右边的也不是一个变量

改正：
int input_value;
cin >> input_value;

(b)
列表初始化如果存在丢失信息的风险，则会报错
改正：double i = {3.14};

(c)
定义时的=是初始化的意思，不是赋值，不能传递。
改正：
double salary = 9999.99，wage = 9999.99;

(d)
int i = 3.14正确，会自动进行类型转换