int的最小尺寸是16位（二进制数的一位所包含的信息就是一比特），大多的计算机中一个字节有8比特（即8位2进制数）
int占2个字节
short 2个 long 4个 long long 8个


无符号就是没有正负的，如unsigned int就是无符号的int类型，在16位系统下int占16位，就可以表示0~65535之间的数字
有符号类型就是有正负的，如(signed) int就是有符号的int类型
C++标准规定了在表示范围内正值和负值量应该平衡。

float 6位有效数字 double 10位有效数字
float 1个字（32比特） double 2个字（64比特）